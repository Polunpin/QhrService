<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qhr.dao.UserEnterpriseRelationMapper">

    <sql id="Relation_Columns">
        id, enterprise_id, user_open_id, role, created_at
    </sql>

    <select id="getByUserEnterprise" resultType="com.qhr.model.UserEnterpriseRelation">
        SELECT <include refid="Relation_Columns"/>
        FROM zj_user_enterprise_relation
        WHERE deleted = 0
        AND user_open_id = #{userOpenId}
        AND enterprise_id = #{enterpriseId}
        LIMIT 1
    </select>

    <update id="restoreByUserEnterprise">
        UPDATE zj_user_enterprise_relation
        SET deleted = 0
        WHERE deleted = 1
          AND user_open_id = #{userOpenId}
          AND enterprise_id = #{enterpriseId}
    </update>


    <update id="deleteByUserEnterprise">
        UPDATE zj_user_enterprise_relation
        SET deleted = 1
        WHERE deleted = 0
          AND user_open_id = #{userOpenId}
          AND enterprise_id = #{enterpriseId}
    </update>

</mapper>
